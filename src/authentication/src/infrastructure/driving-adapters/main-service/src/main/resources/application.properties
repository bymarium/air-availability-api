# --- Base de datos Mongo --- #
spring.data.mongodb.uri=mongodb+srv://carlosCua:carlos123@cluster0.mlp20.mongodb.net/Dune?retryWrites=true&w=majority&appName=Cluster0

# --- Base de datos MySQL --- #
spring.datasource.url=jdbc:mysql://localhost:3306/airsofka
spring.datasource.username=root
spring.datasource.password=root

# --- MySQL Driven --- #
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- Hibernate (JPA) --- #
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true 

# --- SPRING SECURITY OAUTH2 --- #
spring.security.oauth2.client.registration.google.client-id=${client_ID}
spring.security.oauth2.client.registration.google.client-secret=${client_ID_secret}

# --- JWT --- #
# Palabra secreta
security.jwt.secret=${JWT_SECRET}
security.jwt.issuer= Main
# Caducidad del inicio de sesion (horas * 3600000)
security.jwt.ttlMillis=${JWT_TTL_MILLIS}